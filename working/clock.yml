---
- name: asdf
  hosts: ios
  gather_facts: no
  connection: network_cli
  ansible_network_os: cisco.ios.ios
  
  tasks:
  - name: show clock
    ios_command:
      commands: show clock
    register: clock
  
  - debug: msg="{{ clock.stdout}}"
