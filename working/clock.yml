---
- name: asdf
  ansible_network_os: cisco.ios.ios
  hosts: ios
  gather_facts: no
  connection: network_cli
  
  tasks:
  - name: show clock
    ios_command:
      commands: show clock
    register: clock
  
  - debug: msg="{{ clock.stdout}}"
